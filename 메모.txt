예외 발생
raise 예외_클래스 - 8장 자세히
##############
라이브러리: 개발자가 모듈의 함수를 실행하는 것
프레임워크: 모듈이 개발자의 함수를 실행하는 것

패키지(모듈+모듈+...+모듈)
##############

객체(object): 속성 + 메소드
메소드(method): 클래스 함수
인스턴스(instance): 클래스 객체

생성자(constructor): 클래스 이름과 같은 함수
소멸자(destructor): 인스턴스 소멸될 때 호출되는 함수

추상화(abstraction): 프로그램에서 필요한 요소만을 사용해서 객체를 표현하는 것

캡슐화(encapsulation): 객체의 필드, 메소드 클래스 외부에서 실행할 수 없게 감추는 것
필드, 메소드 캡슐화

프라이빗 변수(private variable): 캡슐화를 위해 만들어진 문버

가비지 컬렉터

상속(inheritance): 상위 객체를 기반으로 하위 객체를 생성하는 관계
오버라이드(override): 부모에서 상속받은 메소드를 자식 클래스에서 다시 정의하는 것

클래스(class): 객체 설계도
class 클래스:
    실행문